{
  "name": "khashika-image-fix",
  "version": "1.0.0",
  "description": "Solution bullet-proof pour extraire les images Khashika depuis la source de vérité",
  "scripts": {
    "extract": "npx tsx extract-images-bulletproof.ts",
    "update-json": "npx tsx update-json.ts",
    "sync": "npx tsx sync-supabase.ts",
    "all": "npm run extract && npm run update-json && npm run sync"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.6.0",
    "typescript": "^5.3.0"
  }
}
